apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      initContainers:
        - name: wait-rabbitmq
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z rabbitmq 5672; do sleep 1; done"]
      containers:
        - name: notification-service
          image: eventhub-main-notification-service:latest
          imagePullPolicy: Never
          command: ["python", "main.py"]
          ports:
            - containerPort: 9100
              name: health
          envFrom:
            - configMapRef:
                name: eventhub-config
          env:
            - name: HEALTH_PORT
              value: "9100"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9100
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
