[pytest]
testpaths = tests services/booking-service/booking_service_tests
markers =
    unit: fast isolated unit tests, no external services
    contract: event payload schema checks
    integration: tests that require a live Redis instance
    e2e: full booking flow through all running services
    performance: latency assertions against the running stack
    security: static security scan via bandit
